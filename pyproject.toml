[project]
name = "pyStrich"
version = "0.15"
description = "PyStrich is a Python module to generate 1D and 2D barcodes: Code 39, Code 128 (including GS1-128), EAN-13, Data Matrix (including GS1 Data Matrix), QR Code, PDF417 and Aztec Code."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [
    { name = "Michael Mulqueen", email = "mike@method-b.uk" },
    # Previous authors not listed here. Refer to git log or https://www.method-b.uk/pyStrich/docs/contributors.html
]
maintainers = [
    { name = "Michael Mulqueen", email = "mike@method-b.uk" },
]
keywords = [
    "barcode",
    "qrcode",
    "datamatrix",
    "ean13",
    "code39",
    "code128",
    "pdf417",
    "aztec",
    "gs1",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Office/Business",
]
dependencies = [
    "pillow>=10.3; python_version < '3.11'",
    "pillow>=12.2; python_version >= '3.11'",
    "typing-extensions>=4.15.0",
]

[project.urls]
Homepage = "https://github.com/mmulqueen/pyStrich"
Repository = "https://github.com/mmulqueen/pyStrich"
Documentation = "https://www.method-b.uk/pyStrich/docs/"

[project.scripts]
pystrich = "pystrich.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "mypy>=1.13",
    "ezdxf>=1.4.4",
    "fonttools>=4.55",
    "ruff>=0.15.12",
    "zxing-cpp>=2.2",
    "hypothesis>=6.155.2",
]
docs = [
    "sphinx>=8.1",
    "furo>=2024.8.6",
    "sphinx-copybutton>=0.5",
    "sphinx-argparse>=0.5",
    "sphinxext-opengraph>=0.13.0",
    "sphinx-sitemap>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pystrich"]
exclude = [
    "**/test_*.py",
    "**/conftest.py",
    "**/test_img",
    "pystrich/_simulate_damage.py",
]

[tool.hatch.build.targets.sdist]
include = [
    "pystrich",
    "README.md",
    "LICENSE",
]

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
    # Generated glyph data.
    "pystrich/_courier_glyphs.py",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "RUF", # ruff-specific
]

[tool.ruff.lint.per-file-ignores]
# Test fixtures contain long expected hex/encoded strings that don't wrap cleanly.
"**/test_*.py" = ["E501"]

[tool.mypy]
files = ["pystrich"]
python_version = "3.10"
check_untyped_defs = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
strict_equality = true
extra_checks = true

